.push()- добавить эллемент в конец массива

.pop() - удалить последний эллемент в массиве
чтобы узнать что было удалено, можно так:
let a = [2,7]
let b = a.pop();

.shift()/unshift() - удалить первый / добавить первый
остальные эллементы сдвинутся и изменят свой индекс

.split() - превратить строку в массив
напр. 
let str = "Word1, word2, word3");
let mass = str.split(",");

.trim() - удалить пробелы

.join() - из массива делает строку с указанным разделителем
let mass = ["word1", "word2", "word3"];
mass.join(" "); //указываем разделитель пробел
console.log(mass); // word1 word2 word3

.slice() - добавляет в новый массив другой массив
напр.
let mass = ["word1", "word2", "word3"];
let j = mass.slice(0,1) // j = ("word1", "word2")

деструктуризация массива - чтобы присвоить эллементы массива
в отдельные переменные
напр: 
let a = [2,7];
let [b,c] = a;
console.log(b,c);     // 2,7







